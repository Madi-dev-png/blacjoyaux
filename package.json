{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "images:opt": "npx imagemin \"public/images/*.{jpg,png,jpeg}\" --plugin=webp --out-dir=public/images && npx imagemin \"storage/app/public/**/*.{jpg,png,jpeg}\" --plugin=webp --out-dir=storage/app/public"
    },
    "devDependencies": {
        "imagemin-cli": "^8.0.0",
        "imagemin-webp": "^8.0.0",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
